from setuptools import setup, find_packages

setup(
    name="pry-jupiter-phising-detect-lib-shared-kernel",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Add any dependencies the shared kernel itself needs here
    ],
)
